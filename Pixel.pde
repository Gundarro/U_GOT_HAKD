class Pixel {
    color pColor;
    PVector position;
    PVector rotation;

    Pixel (color pColor, PVector position, PVector rotation) {
        this.pColor = pColor;
        this.position = position;
        this.rotation = rotation;
    }
}
